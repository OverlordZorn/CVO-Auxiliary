class CfgSounds
{
	sounds[] = {}; // OFP required it filled, now it can be empty or absent depending on the game's version

	class cvo_ula_place_1
	{
		name = "Place 1";						// display name
		sound[] = { "z\cvo_aux\addons\ula\data\place_1.ogg", 1, 2, 20 };	// file, volume, pitch, maxDistance
		titles[] = { 0, "" };			// subtitles

		forceTitles = 0;			// Arma 3 - display titles even if global show titles option is off (1) or not (0)
		titlesStructured = 0;		// Arma 3 - treat titles as Structured Text (1) or not (0)
	};
    	class cvo_ula_place_2
	{
		name = "Place 2";						// display name
		sound[] = { "z\cvo_aux\addons\ula\data\place_2.ogg", 1, 2, 20 };	// file, volume, pitch, maxDistance
		titles[] = { 0, "" };			// subtitles

		forceTitles = 0;			// Arma 3 - display titles even if global show titles option is off (1) or not (0)
		titlesStructured = 0;		// Arma 3 - treat titles as Structured Text (1) or not (0)
	};
	class cvo_ula_place_3
	{
		name = "Place 3";						// display name
		sound[] = { "z\cvo_aux\addons\ula\data\place_3.ogg", 1, 2, 20 };	// file, volume, pitch, maxDistance
		titles[] = { 0, "" };			// subtitles

		forceTitles = 0;			// Arma 3 - display titles even if global show titles option is off (1) or not (0)
		titlesStructured = 0;		// Arma 3 - treat titles as Structured Text (1) or not (0)
	};
	class cvo_ula_place_4
	{
		name = "Place 4";						// display name
		sound[] = { "z\cvo_aux\addons\ula\data\place_4.ogg", 1, 2, 20 };	// file, volume, pitch, maxDistance
		titles[] = { 0, "" };			// subtitles

		forceTitles = 0;			// Arma 3 - display titles even if global show titles option is off (1) or not (0)
		titlesStructured = 0;		// Arma 3 - treat titles as Structured Text (1) or not (0)
	};
	class cvo_ula_broke
	{
		name = "Broke";						// display name
		sound[] = { "z\cvo_aux\addons\ula\data\pickaxe_broke.ogg", 1, 2, 20 };	// file, volume, pitch, maxDistance
		titles[] = { 0, "" };			// subtitles

		forceTitles = 0;			// Arma 3 - display titles even if global show titles option is off (1) or not (0)
		titlesStructured = 0;		// Arma 3 - treat titles as Structured Text (1) or not (0)
	};
};